# build command to create exe

go build <folder>/<file>.go

# run command
go run <folder>/<file>.go

# notes:
1. always create a folder, which we call as a package.
2. create the "main package" in the file.
